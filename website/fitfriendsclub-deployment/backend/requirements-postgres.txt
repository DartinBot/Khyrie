# Optional PostgreSQL Dependencies
# Install these only when you need PostgreSQL support in production

# Use binary wheel to avoid pg_config issues
psycopg2-binary>=2.9.0

# Installation command:
# pip install --only-binary=psycopg2-binary psycopg2-binary

# Alternative for production (if you have PostgreSQL development libraries):
# brew install postgresql  # On macOS
# sudo apt-get install postgresql-dev  # On Ubuntu/Debian
# pip install psycopg2>=2.9.0