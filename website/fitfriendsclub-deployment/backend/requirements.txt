# Core Flask Dependencies
flask==3.0.3
flask-cors==5.0.0
werkzeug==3.0.6

# JWT Authentication
python-jose==3.4.0
PyJWT==2.9.0

# Image Processing
Pillow==10.4.0
python-multipart==0.0.20

# HTTP Requests
requests==2.32.4

# Environment Configuration
python-dotenv==1.0.1

# Production Server
gunicorn==23.0.0

# Build Tools
setuptools>=49.0.0
wheel==0.45.1

# PostgreSQL Support for Production (BINARY ONLY - NO SOURCE COMPILATION)
psycopg2-binary==2.9.9
# Explicitly exclude source psycopg2 to prevent compilation attempts
--only-binary=psycopg2