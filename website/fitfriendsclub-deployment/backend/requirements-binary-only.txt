# BINARY-ONLY REQUIREMENTS - NO SOURCE COMPILATION ALLOWED
# This file is specifically designed to prevent pg_config errors

# Core Flask Dependencies
flask==3.0.3
flask-cors==5.0.0
werkzeug==3.0.6

# JWT Authentication
python-jose==3.4.0
PyJWT==2.9.0

# Image Processing
Pillow==10.4.0
python-multipart==0.0.20

# HTTP Requests
requests==2.32.4

# Environment Configuration
python-dotenv==1.0.1

# Production Server
gunicorn==23.0.0

# Build Tools
setuptools>=49.0.0
wheel==0.45.1

# PostgreSQL Support - BINARY ONLY (NO pg_config needed)
psycopg2-binary==2.9.9

# NOTE: This file contains ONLY psycopg2-binary, never psycopg2
# If you see pg_config errors, use this file instead of requirements.txt