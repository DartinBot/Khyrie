# Production deployment requirements (no PostgreSQL dependencies)
flask==3.0.3
flask-cors==5.0.0
python-dotenv==1.0.1
requests==2.32.4
PyJWT==2.9.0
gunicorn==21.2.0

# Optional PostgreSQL support (only install if needed)
# Uncomment the line below only if you want PostgreSQL support
# psycopg2-binary==2.9.9