import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

// Create Vue app
const app = createApp(App)

// Mount the app
app.mount('#app')

// Log successful initialization
console.log('🚀 FitFriendsClub Vue.js app initialized successfully!')
console.log('📱 App features: Header component, responsive design, fitness tracking')